
const TokenType = require('./token-type.js')
const Token = require('./token')
let ReservedKeyword = {
    mixin: 'mixin',
    include: 'include',
    if: 'if',
    use: 'use',
    else: 'else'
}
module.exports = { ReservedKeyword }